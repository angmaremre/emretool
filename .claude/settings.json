{
  "permissions": {
    "allow": [
      "Bash(.venv/bin/python -m app.main)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command'); echo \"$cmd\" | grep -Eq 'pip +(install|uninstall)' && (cd /Users/emre.korkmaz/worksmy/emretool && .venv/bin/python -m pip freeze > requirements.txt) 2>/dev/null; true",
            "statusMessage": "requirements.txt güncelleniyor"
          }
        ]
      }
    ]
  }
}
